## 变更目标

本分支集中解决 TUI footer 输出区与流式输出体验的系列问题：

1. **文本流式完全丢失**：`aizen-core` 丢失 `text_delta` 处理（新增 auth_notice 分支时误删），`snapshot.streamingText` 恒为空，助手回复只能整轮跑完后一次性上屏。
2. **耗时统计显示缺陷**：耗时拼接在流式文本末尾，超宽末行触发换行时会被挤出显示区；流式开始前与纯工具阶段完全不可见。
3. **高频流式卡顿**：每个 delta 触发多次全量快照克隆与历史重算，随会话与回复长度线性放大，真实场景累积到秒级冻结（表现为"耗时 2s 处卡顿几秒"）。
4. **运行中输入区消失**：`status` 非 idle 时输入区连同两条分割线整体隐藏，流式期间无法预览拟发送内容。
5. **已归档内容在 footer 占位**：思考/文本/工具调用进入历史后仍在输出区重复显示，直到轮次结束。

## 主要变更

### footer 布局重构（b5cccd2、6887f1a）
- footer 布局常量收敛至 `footer-layout.ts` 单一来源，消除跨文件常量耦合与重复代码。
- 会话状态文本提为模块常量；输出区换行规范化提取公共函数。

### 流式与历史显示（4a736ef、df453be、7d0cc82）
- 工具组块按思考/对话段边界分批归档，消除最终回复时一次性刷出。
- **恢复 core 对 `text_delta` 的累积**，footer 恢复逐字流式。
- **归档即清空**：assistant 消息归档时清空 `streamingText`/`streamingThinking`；工具结果归档时按 `callId` 从 `activeTools` 移除，历史工具组块成为唯一展示；轮次结束兜底清空保留。

### 耗时统计（e8d4e0a、01b1da9、1a62054）
- 第二条分割线改为右对齐，输出区空内容时收缩高度。
- 耗时与生成 token 统计**移入第二条分割线**（状态文本后拼接，右端与标题对齐），窄终端优先保留状态、从右截断；输出区不再承载统计显示，超宽流式文本不再影响任何统计。

### 卡顿修复（dd7aa37）
- 流式/工具事件快照通知按 **16ms 帧窗口合并**，同一窗口内的一批 delta 只克隆并通知一次。
- 流式期间（transcript 与折叠偏好未变）chat-view **跳过历史全量重算**。
- 实测：60 轮会话 + 300 delta 密集回复，事件循环同步阻塞 335ms → 28ms，阻塞 tick 归零。

### 输入区（7834c45）
- 运行中输入区**保持可见可输入**、回车发送被拦截、输入文字切换为暗淡主题色；仅交互菜单（overlay）与退出时隐藏输入区。

## 兼容性影响

- 会话文件格式不变；快照字段语义变化：`streamingText`/`streamingThinking` 由"轮次级累积"改为"当前未归档段"，`activeTools` 由"轮次级累积"改为"进行中工具"。
- 极端边界：一轮回复在 16ms 内完成时，中间流式快照合并到最终通知（真实 LLM 场景 TTFT 即数百 ms 起步，不会触发）。
