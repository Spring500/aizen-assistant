# AI Agent 行为指令（AizenAssistant）

本文件是 agent 在 AizenAssistant 项目中工作的行为总纲。完整技术方案与推进路线保存在本地私有目录 `.private/`，禁止加入版本库；如本地不存在，开始实质工作前应向项目负责人确认。

---

## 项目概述

AizenAssistant 是基于 [pi](https://github.com/earendil-works/pi) 框架构建的 AI Agent 桌面应用：在保留 pi 原生终端体验的同时，提供一套重点打磨的图形界面（GUI），并对 Agent 的上下文组织方式做深度定制（每轮临时上下文注入、视图式提示词组织）。

技术架构：

- **桌面外壳**：Tauri（Rust），使用系统 WebView，包体轻量、跨平台。
- **GUI 前端**：TypeScript，承载全部界面逻辑。
- **Agent 运行时**：pi，以 `--mode rpc` 作为子进程后端，经 stdin/stdout 的 JSONL 与前端通信。
- **定制逻辑**：pi 扩展（TypeScript），实现两条核心定制诉求。

---

## 强制规则

### 语言选型原则

- 语言选择以 **AI 模型对语言的语料亲和度**为准，优先 TypeScript。
- 业务逻辑集中在 TypeScript 侧（前端 + pi 扩展）；Rust 侧仅做进程管理与 JSONL 转发，**近乎零业务逻辑**。

### 语言规范

- 所有代码注释、新增/修改的文档、与用户的对话必须使用**中文**。
- 变量名、函数名、类名等标识符保持英文。

### 汇报规范

进行**任何口头汇报（对话回复）或书面汇报（报告、方案、文档、注释）**时，必须遵循同目录 [TALK_GUIDE.md](TALK_GUIDE.md)。

> 采用相对路径 + 自然语言引用，而非特定 harness 的 import 语法，以保证在 pi 与其它 harness 下都成立。若当前 harness 支持文件 import，可自行将 TALK_GUIDE.md 纳入常驻上下文。

### 依赖 pi 的纪律

pi 处于 0.x 阶段，发布频繁、破坏性变更较多。依赖它时必须：

- **锁定精确版本**，禁止使用 `^` / `~` 自动升级。
- **薄封装（adapter）**：将对 pi API 的调用收拢到少数文件，升级时只改这一层。
- **不追最新版**：停留在验证过的版本，按周期批量升级，升级前审阅 CHANGELOG 的破坏性变更章节。
- **优先运行时消费**（扩展 / RPC）而非直接依赖 SDK 内部类型，破坏面更小。
- **不修改 pi 源码**。如确需修改，须包裹修改区域并注明原因。

### 验证要求

Agent harness 的正确性无法仅靠单元测试确认，需跨多轮观察实际行为：

- 临时上下文注入：确实进入当轮请求，且下一轮确实消失、未累积。
- 视图切换：装载的 Skill 与系统提示词与预期一致。
- 管道层：JSONL 分帧在含 `U+2028` / `U+2029` 等边界字符时不断裂；sidecar 异常退出可被检测与恢复。
- 分发层：安装包在无 Node 环境的机器上可正常运行。

---

## 上下文获取

- 开始任何实质工作前，先阅读本地私有目录 `.private/` 下的技术方案与路线图建立整体认知；如本地不存在，应向项目负责人确认。
- 涉及 pi 的能力边界时，以 pi 官方文档与随包 `.d.ts` 类型声明为准，不臆测 API。
- 涉及某个模块时，先检查其目录下是否存在 `AGENTS.md` 或 `README.md`。
